import styles from '@/styles/dashboard/index.module.scss';

const DashboardPage = () => {
  console.log();

  return (
    <section className={styles.dashboard}>
      <div className={`container ${styles.dashboard__container}`}>d</div>
    </section>
  );
};

export default DashboardPage;
